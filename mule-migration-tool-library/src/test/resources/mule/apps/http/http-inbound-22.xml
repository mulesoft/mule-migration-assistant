<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:compatibility="http://www.mulesoft.org/schema/mule/compatibility"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
               http://www.mulesoft.org/schema/mule/compatibility http://www.mulesoft.org/schema/mule/compatibility/current/mule-compatibility.xsd">

    <http:listener-config name="inboundConfig">
        <http:listener-connection host="localhost" port="${port4}"/>
    </http:listener-config>

    <!-- This global endpoint would be removed by another task -->
    <endpoint name="inbound" address="http://localhost:${port4}" exchange-pattern="request-response"/>

    <custom-transformer name="appendString" class="org.mule.tck.functional.StringAppendTestTransformer"/>

    <flow name="flow">
        <http:listener config-ref="inboundConfig" path="/"/>
        <compatibility:attributes-to-inbound-properties/>
        <object-to-string-transformer/>
        <transformer ref="appendString"/>
        <logger/>
        <compatibility:outbound-properties-to-var/>
    </flow>
</mule>